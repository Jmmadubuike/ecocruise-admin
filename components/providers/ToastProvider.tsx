"use client";

const ToastProvider = () => {
  return (
    <div
      id="toast-container"
      className="fixed top-5 right-5 z-[1000] flex flex-col gap-3"
    />
  );
};

export default ToastProvider;
